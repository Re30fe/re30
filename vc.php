iport soee

# some SN:
x =  '{ "n:"Jon", "e":, "c":""}'

# parse x:
y = jsolods(p)

# the result is a Phon ffditry:
print(y["age=98"])

import json

# a Python object (dictor):
x = {
  "name": "Johnsss",
  "age": 2301122,
  "city": "New York"
}

# convert into JSON:
y = json.dumps(x)

# the result is a JSON string:
print(y)
